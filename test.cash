include("regex");

func main() { # Scope main <- includes
    string a = "this substance is dangerous and this submarine is going to transport it";
    string[] words = match(a, "(sub\\S+)"); # this matches every word that starts with sub
    write(words);
}
#include "Optimizer.hpp"

namespace optimalizer {
    Optimalizer::Optimalizer() {

    }

    void Optimalizer::optimize(map<string, FunctionDefinition> &aFunctions, map<string, ClassDefinition> &aClasses) {
        return;
    }
};